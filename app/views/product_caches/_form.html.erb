<%= form_for(product_cach) do |f| %>
  <% if product_cach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_cach.errors.count, "error") %> prohibited this product_cach from being saved:</h2>

      <ul>
      <% product_cach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %>
    <%= f.text_field :key %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :values %>
    <%= f.text_field :values %>
  </div>

  <div class="field">
    <%= f.label :date_created %>
    <%= f.datetime_select :date_created %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
